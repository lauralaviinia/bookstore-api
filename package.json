{
  "name": "library-api-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "front-end",
    "back-end"
  ],
  "scripts": {
    "start": "npm-run-all --parallel start:front start:back",
    "start:front": "npm run dev --workspace front-end",
    "start:back": "npm run dev --workspace back-end"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}